# Producción en Vercel: usamos el rewrite definido en vercel.json
# (/api/* → https://clippingapi-production.up.railway.app/*),
# así el navegador ve todo como same-origin y evitamos CORS.
VITE_API_BASE_URL=/api
